@model IEnumerable<TaskA.Models.BookTool>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tasker.Tasker_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tool_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tasker.Tasker_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tool_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookTool_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookTool_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookTool_ID })
        </td>
    </tr>
}

</table>
