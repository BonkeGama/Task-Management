@model IEnumerable<TaskA.Models.VehicleModel>

@{
    ViewBag.Title = "Index";
}
<br />
<br />

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="box box-primary">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title"><b style="color:#dd4b39; text-transform:capitalize;margin-left:43%;"><i class="fa fa-user-plus"></i>Vehicle Model</b></h3>
        </div>
        <div class="panel-body">
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Vehicle_model)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Vehicle_model)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.Model_ID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.Model_ID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Model_ID })
                        </td>
                    </tr>
                }

            </table>

        </div>
    </div>
</div>
