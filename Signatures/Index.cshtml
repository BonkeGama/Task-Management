@model IEnumerable<TaskA.Models.Signature>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ToolSet.Tool_SetName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MySignature)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ToolSet.Tool_SetName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MySignature)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Signature_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Signature_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Signature_ID })
        </td>
    </tr>
}

</table>
